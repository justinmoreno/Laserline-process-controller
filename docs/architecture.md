# Architecture

This document describes the overall architecture of the Laserline process controller.

- `app.py` is the application entry point.
- `gui/` contains the graphical user interface components.
- `laser/` contains laser control and simulation logic.
- `drivers/` contains hardware driver interfaces.
- `logger/` contains data logging utilities.
- `docs/` contains supporting documentation.
